[package]
name = "pitboard"
version = "0.0.0"
edition = "2024"
rust-version = "1.98"
license = "Apache-2.0"
description = "Park and restore your own Claude Code logins on one machine, and see what each one has left."
repository = "https://github.com/ngoquocdat/pitboard"
homepage = "https://github.com/ngoquocdat/pitboard"
keywords = ["cli", "keychain", "credentials", "macos", "linux"]
categories = ["command-line-utilities"]
readme = "README.md"
exclude = ["/.github", "/packaging", "/deny.toml", "/clippy.toml", "/rustfmt.toml"]

[lib]
name = "pitboard"
path = "src/lib.rs"

[[bin]]
name = "pitboard"
path = "src/main.rs"

[dependencies]
# Deliberately small. Every addition needs a reason in the PR description.
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
sha2 = "0.10"
unicode-normalization = "0.1"
libc = "0.2"
thiserror = "2"
machine-uid = "0.6"
filetime = "0.2"
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
clap_mangen = "0.3"
# TLS: rustls for the protocol, the operating system for deciding which certificates to
# trust. No bundled root store, so a corporate or system trust setting is honoured.
ureq = { version = "3", default-features = false, features = ["rustls-no-provider", "platform-verifier"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
undocumented_unsafe_blocks = "warn"
